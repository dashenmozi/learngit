关于使用碰到的问题记录

1.一切都配置好了，jrebel日志也并没有显示出任何问题，但是无法生效。
在stackoverflow上发现是使用方式错误了。在当前方法的循环中无法直接修改代码生效，必须在循环中调用一个方法，在调用的方法中修改代码才可以生效。
如下所示：
  for (; ;) {
            System.out.println("Hello2");
            foo();
//            Thread.sleep(1000);
        }
直接修改打印里的内容是不行的。
以后遇到类似情况需要考虑使用方式是否错误，不要走错了方向。
